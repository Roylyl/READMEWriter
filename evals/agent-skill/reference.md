# IssueBrief

技能输出包括观察到的行为、预期行为与复现步骤。

## 安装与调用

将 `SKILL.md` 和 `references/` 复制到宿主的 `skills/issue-brief/` 目录；默认 Codex 目录可由 `CODEX_HOME` 定位。使用 `$issue-brief` 并附上问题描述。

入口见 [SKILL.md](SKILL.md)。这份夹具没有宿主调用评估记录，也没有指定项目许可证，不能补写 MIT。
